java TwitterKMeansClustering  <numberOfClusters> <full path to initialSeedsFile> <full path to TweetsDataFile> <outputFile>

Tweet clustering using the Jaccard Distance metric and K-means clustering algorithm to cluster redundant/repeated tweets into the same cluster. 
  
